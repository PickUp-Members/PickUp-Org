# TODO - AdminDashboard front-end fixes

- [ ] Fix JSX/tab rendering in `frontend/src/Pages/Admin/AdminDashboard.js` so “Seller Verification” tab becomes clickable and renders correctly.
- [ ] Harden seller requests fetching in the same file (guard for missing token/user, keep loading correct).
- [x] Verify Admin → Seller Verification fetches from `/admin/seller-requests` and table renders.
- [x] Verify approve/reject removes rows.


